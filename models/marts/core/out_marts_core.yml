version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_id
        tests:
            - unique
            - not_null

  - name: fct_orders
    columns:
      - name: customer_id
        tests:
          - not_null
          - relationships:
              field: customer_id
              to: ref('dim_customers')
